SUPABASE_URL=https://seu-projeto.supabase.co

# Settings → API → service_role (SECRET) — nunca use sb_publishable_ aqui
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_secret

# Opcional: chave pública (publishable/anon) — exige políticas RLS nas tabelas
# SUPABASE_ANON_KEY=sua_chave_publica

PORT=3001
